# 项目部署与同步指南

本文档包含两部分：
1.  **如何将项目部署到 Cloudflare Pages**
2.  **如何与上游 GitHub 仓库保持同步**

> 本项目修改自：https://github.com/tina-hello/doh-cf-workers

---

## 🚀 Part 1: 部署到 Cloudflare Pages

本指南将引导你使用 Wrangler CLI 将静态项目一键部署到 Cloudflare Pages。

### 1. 准备工作

在开始之前，请确保你已完成以下设置：

*   ✅ **安装 Node.js**: 建议使用 LTS 版本。
*   ✅ **安装 Wrangler**: Cloudflare 的命令行工具。
    ```powershell
    npm install -g wrangler
    ```
*   ✅ **拥有 Cloudflare 账号**: 并准备创建一个 Pages 项目。
*   ✅ **项目代码在本地**: 确保项目文件已克隆或下载到你的电脑。

### 2. 部署步骤

#### 第一步：登录 Cloudflare

在终端（PowerShell）中运行以下命令，它会打开浏览器引导你完成授权。

```powershell
wrangler login
```

#### 第二步：进入项目目录

使用 `cd` 命令切换到你的项目文件夹。

#### 第三步：执行部署命令

```powershell
wrangler deploy
```

---

## 🔄 Part 2: 与上游 GitHub 仓库保持同步

当你将别人的公开项目上传到自己的仓库后，可以通过以下步骤轻松地从原作者（上游）那里获取更新。

### 1. 初始化设置 (仅需一次)

#### 第一步：建立远程仓库连接

进入你的本地项目目录，我们将把**原始仓库**设置为 `upstream`，把你**自己的仓库**设置为 `origin`。

```powershell
# 1. (如果已存在) 重命名原始的 origin 为 upstream
git remote rename origin upstream

# 2. 添加你自己的 GitHub 仓库为新的 origin
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# 3. 验证设置
git remote -v
```
你应该会看到 `upstream` 指向原始仓库，`origin` 指向你自己的仓库。

#### 第二步：推送代码到你的仓库

将本地的 `main` 分支推送到你的 GitHub 仓库，并建立跟踪关系。

```powershell
git push -u origin main
```

### 2. 日常同步流程

当你需要获取原始仓库的最新更新时，执行以下操作。

#### 第一步：从上游拉取最新代码

此命令会获取 `upstream` 仓库的所有最新数据，但不会修改你的本地代码。

```powershell
git fetch upstream
```

#### 第二步：合并更新到你的分支

切换到你的主分支，然后将上游的更新合并进来。

```powershell
git checkout main
git merge upstream/main
```
**注意**: 如果你本地有修改，合并时可能会产生**冲突 (Conflict)**。你需要根据提示手动解决这些冲突，然后提交更改。

#### 第三步：将更新推送到你的仓库

合并完成后，将最新的代码推送到你自己的 GitHub 仓库。

```powershell
git push origin main
```

通过以上流程，你就可以在维护自己版本的同时，持续与原始项目保持同步。